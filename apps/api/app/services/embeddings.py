# Placeholder: plug in your embeddings client and Qdrant.
# Use QDRANT_URL, QDRANT_API_KEY from env via app.core.config.

# def embed(text: str) -> list[float]:
#     ...
# def search(query_embedding: list[float], limit: int = 10):
#     ...
